import { motion } from "framer-motion";

const Background = () => {

    return (
        <div className="w-full h-full bg-primary fixed flex justify-center items-center">
           <div>
           
           </div>
        </div>
    )
}


export default Background